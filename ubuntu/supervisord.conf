[supervisord]
nodaemon=true
logfile=/var/log/supervisor/supervisord.log
pidfile=/var/run/supervisord.pid
childlogdir=/var/log/supervisor 

[program:confd]
command=/opt/confd.sh
autostart=true
autorestart=true

[include]
files = /etc/supervisor/services.d/*.conf